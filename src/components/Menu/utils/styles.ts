import { makeStyles } from '@material-ui/core';

export default makeStyles({
  root: {
    padding: 0,
    display: 'flex',
    justifyContent: 'space-between',
    flexDirection: 'row',
  },
  leftContainer: {
    display: 'flex',
    alignItems: 'center',
  },
});
